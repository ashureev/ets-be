JAVA version 17

need sql connection



install Java & Maven

`sudo apt update`

`sudo apt install openjdk-17-jdk -y`

`sudo apt install maven -y`


#### git clone BE

`git clone https://github.com/DevOpsC4/ets-be`


#### Install SQL server

`sudo apt install mysql-server`
